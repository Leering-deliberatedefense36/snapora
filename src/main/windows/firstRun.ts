/**
 * First-run permission wizard window.
 *
 * v0.1 — stub. Implemented as part of v0.1 polish — this is one of the
 * highest-value UX surfaces (see CONTRIBUTING / ROADMAP).
 */
export function maybeShowFirstRunWizard(): void {
  // TODO(v0.1): if no `seenFirstRun` flag in prefs, show wizard window with:
  //   - explanation of permissions
  //   - deeplinks to System Settings
  //   - poll-and-show-current-status UI
  //   - prompt to relaunch after Screen Recording grant
}
