/**
 * Settings window placeholder.
 * v0.1 TODO — wire up preferences (hotkeys, save folder, format, defaults).
 */
export function Settings() {
  return (
    <div className="flex h-full items-center justify-center">
      <div className="text-center text-white/50">
        <h1 className="text-lg font-medium text-white">Settings</h1>
        <p className="mt-2 text-sm">Coming in v0.1 — see ROADMAP.md</p>
      </div>
    </div>
  );
}
